# PagePilot Docker 环境变量示例。
# 首次部署请复制为 .env，然后填入自己的值：
#   cp .env.example .env
#
# 用于加密访问密码、屏幕授权等服务端敏感数据。
# 已上线旧环境：必须继续使用原来的值；如果旧环境从未配置过，
# 兼容旧版本的历史值是 pagepilot-dev-master-key-0000000。
# 全新空库部署：建议生成一个新的固定值。
# 生成示例：
#   openssl rand -base64 32
HOSTCTL_MASTER_KEY=
